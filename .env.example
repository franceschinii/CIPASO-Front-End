# Configuração de Ambiente - Memorial CIPASO
# Copie este arquivo para .env e ajuste os valores

# URL base da API (produção)
VITE_API_URL=https://memorial.cipaso.org.br/api

# Ambiente (development | production)
VITE_ENV=development

# Habilitar logs de debug
VITE_DEBUG=true

# Google Fonts API Key (opcional, se precisar de analytics de fontes)
# VITE_GOOGLE_FONTS_KEY=

# Instruções:
# 1. Copie este arquivo: cp .env.example .env
# 2. Ajuste VITE_API_URL para o domínio de produção
# 3. Em desenvolvimento local, pode deixar comentado para usar /api relativo
# 4. NÃO commitar o arquivo .env (já está no .gitignore)
